# 9618 Paper 4 Practical (Oct/Nov 2022)

## Python 解答得分说明（Question 1–3）

> 依据文档：
> **Cambridge International AS & A Level Computer Science 9618/42**
> **Mark Scheme – October/November 2022**

本文档用于说明 Question 1–3 的 Python 解答如何逐条满足评分标准（Mark Scheme），并明确每一小题的得分依据。

---

## 🟦 Question 1 – Priority Jobs（19 marks）

### 1(a) 声明全局数据结构【3 marks】

**评分点（1 mark per point）：**

- 声明全局二维数组 `Jobs`
- 数组大小为 `100 × 2`
- 声明全局整型变量 `NumberOfJobs`

**得分说明：**
代码中正确声明了用于存储作业编号与优先级的全局二维数组，并使用整型变量记录当前作业数量，完全符合评分标准。

**得分：** ✅ 3 / 3

---

### 1(b) `Initialise()` 过程【3 marks】

**评分点：**

- 正确的过程头
- 遍历数组全部元素
- 所有元素赋值为 `-1`，并将 `NumberOfJobs` 设为 `0`

**得分说明：**
`Initialise()` 过程对数组进行完整初始化，并重置作业计数，逻辑清晰、无遗漏。

**得分：** ✅ 3 / 3

---

### 1(c) `AddJob()` 过程【5 marks】

**评分点（每点 1 分）：**

- 正确的过程头，包含两个整数参数
- 检查数组是否已满
- 数组满时输出 `"Not added"`
- 未满时将数据存入下一个空位
- 成功后递增 `NumberOfJobs` 并输出 `"Added"`

**得分说明：**
过程完整实现容量检查、条件判断、数据存储及输出反馈，完全符合 Mark Scheme 要求。

**得分：** ✅ 5 / 5

---

### 1(d) 主程序调用【2 marks】

**评分点：**

- 调用 `Initialise()`
- 按指定顺序调用 5 次 `AddJob()`，参数正确

**得分说明：**
主程序严格按照题目给定顺序与参数调用过程，无参数或顺序错误。

**得分：** ✅ 2 / 2

---

### 1(e) `InsertionSort()` 排序【5 marks】

**评分点（1 mark per point）：**

- 正确的过程头
- 外层循环遍历已存作业数量
- 内层循环比较 priority（第二列）
- 元素向上移动
- 正确插入当前元素

**得分说明：**
插入排序算法基于优先级进行升序排序，逻辑与示例答案一致。

**得分：** ✅ 5 / 5

---

### 1(f) `PrintArray()` 输出【3 marks】

**评分点：**

- 正确的过程头
- 每个作业一行输出
- 输出格式包含 `priority`

**得分说明：**
输出格式符合题目示例要求，每条作业信息清晰显示。

**得分：** ✅ 3 / 3

---

### 1(g) 主程序排序与输出【2 marks】

**评分点：**

- 正确调用 `InsertionSort()`
- 正确调用 `PrintArray()`
- 测试输出顺序正确

**得分说明：**
主程序按要求完成排序并输出结果，测试结果符合评分标准。

**得分：** ✅ 2 / 2

---

### ✅ Question 1 总得分：**19 / 19**

---

## 🟦 Question 2 – Character 类与游戏逻辑（31 marks）

### 2(a) `Character` 类声明【4 marks】

**评分点：**

- 正确的类声明
- 3 个私有属性（Name, XCoordinate, YCoordinate）
- 构造方法带 3 个参数
- 参数正确赋值给属性

**得分说明：**
类结构与封装完全符合题目要求。

**得分：** ✅ 4 / 4

---

### 2(b) Getter 方法【3 marks】

**评分点：**

- `GetName()`
- `GetX()`
- `GetY()`

**得分说明：**
所有 Getter 方法正确返回对应私有属性。

**得分：** ✅ 3 / 3

---

### 2(c) `ChangePosition()` 方法【2 marks】

**评分点：**

- 正确的方法头，两个整数参数
- 正确累加 X 与 Y 坐标

**得分说明：**
方法正确修改角色位置坐标。

**得分：** ✅ 2 / 2

---

### 2(d) 从文件读取角色数据【7 marks】

**评分点：**

- 声明 10 个 `Character` 对象的数组 / 列表
- 打开 `Characters.txt`
- 循环读取 10 组数据
- 正确实例化 `Character`
- 存储进数组
- 正确关闭文件
- 异常处理（文件不存在）

**得分说明：**
文件读取与对象创建逻辑完整，包含异常处理。

**得分：** ✅ 7 / 7

---

### 2(e) 搜索角色【5 marks】

**评分点：**

- 接收用户输入
- 统一大小写比较
- 遍历角色数组
- 未找到则重复输入
- 存储角色索引位置

**得分说明：**
搜索逻辑完整，能正确定位角色。

**得分：** ✅ 5 / 5

---

### 2(f) 移动角色【7 marks】

**评分点：**

- 循环输入直到合法
- 调用 `ChangePosition()`
- A / W / S / D 对应正确坐标变化

**得分说明：**
方向控制与坐标变化完全符合题目定义。

**得分：** ✅ 7 / 7

---

### 2(g) 输出与测试【3 marks】

**评分点：**

- 输出角色名与新坐标
- 使用 Getter 方法
- 测试结果正确

**得分说明：**
输出格式与测试结果符合要求。

**得分：** ✅ 3 / 3

---

### ✅ Question 2 总得分：**31 / 31**

---

## 🟦 Question 3 – Queue 与递归（25 marks）

### 3(a) 队列声明【3 marks】

**评分点：**

- 100 元素的数组
- HeadPointer 初始化为 -1
- TailPointer 初始化为 0

**得分说明：**
队列结构声明正确。

**得分：** ✅ 3 / 3

---

### 3(b) `Enqueue()` 函数【6 marks】

**评分点：**

- 正确函数头
- 判断队列是否已满
- 第一次入队设置 HeadPointer
- 存入数据
- TailPointer 递增
- 返回布尔值

**得分说明：**
函数逻辑完整，返回值使用正确。

**得分：** ✅ 6 / 6

---

### 3(c) 入队 1–20【4 marks】

**评分点：**

- 循环 20 次
- 使用 `Enqueue()`
- 使用返回值
- 输出成功 / 失败

**得分说明：**
入队逻辑与结果判断符合要求。

**得分：** ✅ 4 / 4

---

### 3(d) 递归函数【6 marks】

**评分点：**

- 正确的递归函数头
- 基本情况
- 返回队列元素
- 递归调用
- 累加结果
- 返回最终值

**得分说明：**
递归版本与给定迭代算法等价。

**得分：** ✅ 6 / 6

---

### 3(e) 调用与测试【2 marks】

**评分点：**

- 调用递归函数并输出
- 输出结果为 210

**得分说明：**
递归求和结果正确。

**得分：** ✅ 2 / 2

---

### ✅ Question 3 总得分：**25 / 25**

---

## 🎯 全卷总结

| Question        | 得分              |
| --------------- | ----------------- |
| Q1              | 19 / 19           |
| Q2              | 31 / 31           |
| Q3              | 25 / 25           |
| **Total** | **75 / 75** |

**结论：**
该 Python 解答在结构、逻辑与输出上均完全满足 Question Paper 与 Mark Scheme 要求，可作为 **满分示范答案** 使用。
